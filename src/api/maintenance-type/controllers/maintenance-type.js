'use strict';

/**
 * maintenance-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::maintenance-type.maintenance-type');
